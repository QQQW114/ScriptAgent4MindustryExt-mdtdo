# MDT DO 项目长期记忆与当前状态

> 更新时间：2026-08-13
> 维护对象：C:\Users\qw114\Desktop\other\mdt保留\mdtdo
> 文档性质：给新会话和接手项目的 Agent 使用的上下文、判断依据与状态面板。

## 0. 先读这里：这份文档怎么用

这不是实现手册，也不是要求每次任务机械执行的固定流程。它的作用是让新 Agent 先理解项目是什么、哪些决策长期有效、当前状态在哪里、用户希望怎样协作，再根据实际任务的风险和范围选择合适的调查、修改、测试和记录方式。

项目状态会变化。发现本文档与当前代码、配置、日志或专项文档不一致时，应以当前事实为准，并在完成核对后更新本文档的相关状态，而不是照搬旧结论。

### 事实优先级

一般按以下顺序判断当前事实：

1. 当前工作区代码、配置、运行日志和实际验证证据；
2. 最新的专项维护文档；
3. 本文档的“当前快照”；
4. 更早的会话、临时任务和历史建议。

旧记录仍然有价值，尤其是“为什么这样设计”和“哪些方案已经被否决”，但历史方案不能自动视为当前实现。

### 生产与证据边界

Agent 可以在本工作区分析、编辑、构建、运行测试副本并准备交付材料；**不能代替用户或运维访问、停启、替换、回滚或确认生产服务器**。生产服不存在于本机，Agent 也无法推送/部署到生产；生产部署、推送与生产实测只能由用户/运维执行并提供证据。

以下状态互不等同，应分别记录：

- 代码或脚本已修改；
- 本地构建、冷启动或测试副本已验证；
- 生产候选文件已准备；
- 生产已部署或已实测；
- 远程仓库已提交或已推送。

建议使用的状态词：当前基线、已本地验证、测试副本验证、生产待确认、生产已观察（附证据）、实验性、待用户/运维执行、已替代、已废弃、未验证。

### 启动与脚本加载日志优先级

2026-07-30 已直接运行 `mdtserver/start-server.ps1` 复核日志链路可用。后续进行冷启动、编译或脚本加载诊断时，优先使用项目自带的正式日志，不再默认在项目根目录生成重定向临时日志：

- `mdtserver/config/logs/log-0.txt`：当前 Mindustry/MindustryX 运行日志，可确认 ScriptAgent 汇总、`Server loaded`、端口开放和地图加载；
- `mdtserver/config/logs/script-load/current.log`：当前脚本逐项加载、编译、数据库初始化和业务模块启动日志；
- `mdtserver/config/logs/last.log`、`mdtserver/config/logs/script-load/last.log`、`last-error.log` 及各自 `history/`：由启动脚本在上一轮归档或进程正常退出后维护，适合复查已结束/上一轮启动。

只有上述正式日志缺失、没有随本轮启动更新、损坏，或确实无法覆盖启动器自身输出时，才创建一次性根目录临时日志；诊断结论写入文档后应及时清理。清理临时产物时不得删除 `config/logs` 正式日志，也不得触碰数据库、外部 CP、缓存备份或其他运行数据。

### 本地服务器启动与命令 Socket 调试要点（2026-08-03 实测）

- 正常启动用 `mdtserver/start-server.ps1`（自带 `-Dfile.encoding=UTF-8`、Java logging 配置与异常退出重启）。`start-server.ps1` 会把 `server.properties` 的 socket 相关项拼成多条 `config socketInput*`（如 `config socketInputPort 6859`、`config socketInputAddress localhost`、`config socketInput true`）追加到启动参数。
- **命令 Socket 竞态**：ServerControl 的 `Trigger.socketConfigChanged` 对每条 `config socketInput*` 执行 `toggleSocket(false)+toggleSocket(true)`；多条连续触发会让 accept 线程在重启竞态中死亡，`log-0.txt` 出现 `[E] Command input socket already in use...` 与 `java.net.SocketException: Socket is not bound yet`，TCP 能连上 6859 但命令无回显。
- **规避**：直接以 java 命令行启动、只传单条 `config socketInput true`（不带 port/address，用默认 6859/localhost），socket server 只启动一次，命令回显正常。
- **命令通道**：`hookGameHandler` 把 `ServerControl.handler` 反射替换为 `MyCommandHandler("", Config.serverCommands)`，命令 Socket 与游戏内 `/sa`、`/cp`、`/externalcp` 共用框架命令根；原生命令仍由原生 handler 执行。已验证 Socket 上 `cp files`、`cp load 11`、`externalcp list` 正常回显（UTF-8）。
- **自动化启动**：`java <JVM参数> -jar server-2026.07.20.B480-mdtdo.jar "config name ...,config port 6567,...,config socketInput true,playerlimit 16,shuffle all,host"`，stdin 走管道即可（JLine 退化为 dumb terminal），命令回显在 stdout 与日志文件。
- **退出与清理**：socket `exit` 在脚本停用后可能不生效；stdin EOF 触发 JLine 退出路径并遇到项目既有的 ScriptAgent 集中卸载停滞，进程可能不退出。清理时结束 java 进程树并确认 6567/6859/10099 端口释放；临时改过的 `server.properties` 要从备份还原。

## 1. 当前快照（最近核对：2026-08-13）

| 项目 | 当前记录 | 解释 |
|---|---|---|
| 工作区 | C:\Users\qw114\Desktop\other\mdt保留\mdtdo | MDT DO 服务器独立开发项目；与 `mdt保留` 下其他项目（mod-dp-bridge、参考项目等）并列，不等同生产目录。 |
| Git 基线 | 本地分支 master；提交号以 `git rev-parse HEAD` 现场核对 | 本文档不嵌入“当前提交号”，避免文档随提交自身变化而循环失效；写新改动前仍需检查 git status。 |
| 游戏/服务端候选 | **当前基线：Mindustry v160.1，MindustryX 正式发行版 X37（`v2026.09.X37`，2026-09-13 起启用；与 prerelease B493 同一提交 `dc388e9`）** | **版本跟踪口径：常态化跟进上游最新版本**（Mindustry/MindustryX/参考项目）；**ScriptAgent 以本项目自行维护为主**，仅在较大变动的发行版更新时评估是否跟进。历史上由用户拍板升级基线；本轮"跟进 X37"由用户明确要求。X36 仍是 v159.7，v160.1 的正式发行版自 X37 起才有。 |
| 基线 JAR | `mdtserver/server-2026.09.X37.jar`（**MindustryX** 发行版，`version.properties` 内 `build=160.1`），已在 `server.properties` 的 `jar=` 显式指定 | SHA-256 `F1CD2B5AFB9ED5395707F228F0848FCE4C0872E14171D4C80941C53226F7B575`；回滚线 `server-2026.09.11.B491.jar`（同 v160.1）与 `server-2026.08.12.B485.jar`（v159.7 最后稳定发行）都保留在目录内，改 `jar=` 即可回滚。脚本硬依赖 MindustryX（`trafficMonitor.kts` → `mindustryX.events.SendPacketEvent`），**不能用官方 Mindustry 的 jar 顶替**。 |
| JAR 与脚本时点 | X37（160.1）于 2026-09-13 完成冷启动验证并设为基线（`start-server.ps1 -DryRun` 已确认选中） | 启动脚本优先读 `server.properties` 的 `jar=`，缺省才按 `server-*.jar` 最新修改时间自动选择——切换版本时务必同步 `jar=`；`jar=` 指向的文件不存在时启动脚本会直接报错而不是静默换 jar。 |
| 脚本运行时 | 工作区脚本已按 ScriptAgent 3.4.0 迁移；2026-07-29 已替换为维护者提供的未发行竞态修复构建 | 构建文件名为 `ScriptAgent4MindustryExt-e4b136c.jar`，运行时显示 `ScriptAgent c2823c1`；旧本地字节码 workaround 已备份。该构建尚未进入正式发行版，也不能推断生产正在使用。 |
| 最近本地证据 | 2026-09-13 **X37 / Mindustry 160.1 冷启动 157/153/148/0**（`ScriptAgent c2823c1`）与 **B491 同脚本集 157/153/148/0** 完全一致，编译错误 0、异常 0、Socket 正常、端口全释放。此前 2026-09-12 B491 冷启动 157/153/148/0（含当时已实装的火焰链路运行期实测，**该实测随 09-13 删除 `limitFire.kts` 作废**）；08-14 B485 冷启动 156/152/147/0 并实测上行网卡统计 | 160 适配前同环境为 `157/131/129/出错22`（`Groups.fire`/`Groups.puddle` 被 160 移除导致 3 个根脚本编译失败并级联）。脚本数由 158 降到 157 是 09-13 删除 `limitFire.kts` 的预期结果。各轮测试进程均已清理、端口释放、生产 `server.properties` 仅按"跟进 X37"改动了 `jar=` 一行（脚本测试走独立副本 `.tmp-160test`）。 |
| 生产状态 | 生产待用户/运维确认（生产服不存在于本机，Agent 无法推送/部署） | Agent 无法完成生产部署、停启、替换、推送或回滚；当前 HEAD 之后的改动尤其不能默认已经部署。 |
| 远程仓库状态 | mdtdo 服务器仓库无远程、不直接推送；推送统一经 `ScriptAgent4MindustryExt-mdtdo`（origin=github.com/QQQW114/ScriptAgent4MindustryExt-mdtdo，upstream=way-zer/ScriptAgent4MindustryExt） | 每次推送前，先把 docs 与 scripts 的改动同步进插件仓库再提交推送；推送默认包含 scripts 与 Agent 开发文档。 |
| 工作区漂移 | 有外部 CP、资产缓存、SA 备份、服务器配置备份等未跟踪运行/临时内容 | 不要把运行数据、缓存、数据库、备份、外部内容或会话提取物自动纳入源码提交。 |
| ScriptAgent4MindustryExt-mdtdo | `mdtdo/ScriptAgent4MindustryExt-mdtdo`：插件脚本开源仓库（独立 Git，main 分支，origin=github.com/QQQW114/ScriptAgent4MindustryExt-mdtdo，upstream=way-zer/ScriptAgent4MindustryExt） | 只发布脚本与文档，不含服务器 JAR/数据库/地图/日志；已被本服务器仓库 gitignore，变更应在其自身仓库内提交。 |
| mod-dp-bridge（同级） | 位于 `mdt保留\mod-dp-bridge` 的独立 Git 仓库（main 分支，origin=github.com/QQQW114/mod-dp-bridge），v0.2.0 | 实验性 Mod→v159.7 DP 转换工具（CLI + 仅 loopback 本地 Web），与 mdtdo 并列、不属于本仓库；用途、边界与产物审阅见其自身 README/CHANGELOG/docs。 |

docs/current-issues.md 的记录时间较早且可能显示“暂无”，不能单独作为全部待办的证明；还要结合最新专项文档、临时任务和会话记录判断未验证项。

## 2. 项目定位与整体地图

MDT DO 是一个完整的 Mindustry 专服产品，而不是若干互不相关的脚本。玩法、账号与数据库、网络同步、资产、权限、运维、运行环境和文档互相影响；遇到单个脚本看不懂时，应扩大到调用链、模块依赖、服务端版本和玩家体验，而不是孤立地修补一处。

主要区域：

- mdtserver/config/scripts：插件总目录；
- mdtserver/config/scripts/mapScript：地图与地图标签玩法；
- mdtserver/config/scripts/coreMindustry：菜单、积分板、基础框架；
- mdtserver/config/scripts/wayzer：主要业务、账号、信任、技能、商店、投票、数据库、权限与保护逻辑；
- docs：维护总览、领域设计、兼容说明、验证记录；
- ScriptAgent4MindustryExt-mdtdo：插件脚本开源仓库（独立 Git 仓库，含 scripts/ 与 docs/）；
- ../参考项目：公用参考项目（官方 Mindustry、MindustryX 和 ScriptAgent 的源码/版本参考），与 mdtdo 并列，不要移动；仓库地址、跟进步骤与网络代理回退（10808 失效则走 7897）见 `../参考项目/README.md`；
- ../mod-dp-bridge：同级 Mod→v159.7 DP 转换工具项目（独立 Git 仓库）。

../参考项目/MindustryX-main 是独立的补丁式参考仓库，目录内有自己的 AGENTS.md；只有在修改该嵌套仓库时再读取并遵循，不能把它误当作 MDT 根项目的固定流程。

运行中的数据库、日志、缓存、外部 CP、音乐、资产和备份属于运行数据或交付材料，和可提交的源码/文档分开管理。

## 3. 用户偏好与协作边界

以下是从用户长期要求中提炼的默认画像，不替代用户在当前任务中的新指示：

- 用户维护的是长期运行的专服产品，首要关心稳定性、状态正确性、玩家能正常进服/重生和既有玩法不被破坏。
- 用户偏好先理解现状、影响面和风险，再高执行力地完成已经对齐的小任务；不喜欢凭猜测边改边试，也不希望为小问题堆叠新系统。
- 架构、版本、协议、数据库、权限、生产和不可逆行为等大范围改动，通常先调查源码/文档、说明取舍和回滚方式，等用户确认后实施。
- 改动完成后希望看到真实的验证证据、未覆盖边界、风险和下一步，而不是笼统的“已完成”。
- 本地修改、测试、提交、远程推送和生产部署是不同动作。用户明确要求提交时再提交；远程推送和生产替换仍应单独确认。生产部署不能由 Agent 完成。
- 用户偏好少引入系统、复用现有结构、保留可回滚路径；对生产锁死、数据损坏、玩家无法进服、旧状态回放和不可逆权限扩大容忍度低。
- 真实数据、封禁记录、数据库、备份和运行文件不应被擅自破坏、清理或混入提交。
- 测试边界（2026-08-13 明确）：除特殊情况与用户另行要求外，验证以服务端冷启动、脚本正确加载为准；运行中可验证的功能性改动在启动后的服务器上用控制台/命令 Socket 实测；不尝试真实客户端测试，也不为服务器手动添加压力；菜单、技能玩法等无法通过控制台测试的改动无需测试，按未覆盖边界记录即可。
- 远程推送流程（2026-08-13 明确）：mdtdo 服务器仓库只做本地维护、不直接推送；每次推送前，**先同步 docs 与 scripts 的改动**到 `ScriptAgent4MindustryExt-mdtdo` 仓库，再从该仓库提交并推送；**推送默认包含 scripts 与 Agent 开发文档（docs）**。
- 提交与推送的当前状态（2026-09-11 用户明确）：**默认提交但不推送**——完成一轮工作后按上述流程把改动提交到 mdtdo 与插件仓库即可，插件仓库可以处于“领先 origin/main 若干提交”的状态；**未经用户明确指示不要执行 `git push`**，也不要为了推送反复询问。
- 版本跟进总体原则（2026-08-13 明确）：**跟进新的稳定版本**；无论 JAR 文件还是 SA 插件，**均由用户决定跟进哪个版本，并跟进用户所要求的对应版本**。文档中的版本号只是“当前时点状态”，Agent 不自行锁定基线、也不因上游更新自动改基线；需要升级时由用户提出并按新版本重新核对。
- 版本跟踪与 SA 维护口径（2026-09-12 明确）：**常态化跟进上游最新版本**——Mindustry / MindustryX / 参考项目持续跟到最新，发现新版本按流程记录并适配；**ScriptAgent 插件以本项目自行维护为主**（不再逐版跟随上游 SA），**只在出现较大变动的发行版更新时再评估是否跟进**。当前基线为 Mindustry v160.1 / MindustryX 正式发行版 `v2026.09.X37`（2026-09-13 起，= prerelease B493 同一提交）。
- 文档跟进规则（2026-08-22 明确）：**每次更新内容都要跟进文档**——凡是代码/脚本行为、配置、指令、版本或功能变化，都需要在对应专项文档与维护记录中同步，不留未记录的变化。
- 汇报以中文、结构化、简洁、可追溯为宜；信息不足的会话如实记录，不为凑篇幅臆测扩写。

“直接做”和“重大改动先确认”并不矛盾：按风险、影响范围和授权范围判断。用户对当前任务的新要求优先于这里的默认偏好。

## 4. 长期设计不变量

这些是帮助 Agent 做取舍的长期边界，不是把每个任务锁成同一套操作步骤。

### 4.1 稳定性与正确性优先

正确的玩家状态、原生生命周期、可靠性和可恢复性优先于表面上的限流效果、单项指标或新功能数量。不能为了“看起来生效”破坏底层顺序、可靠包语义或已有回滚链路。

### 4.2 网络压力与游戏性能分层

总网络流量、世界/资产/音乐/CP 传输、游戏同步、TPS 和实体规模是不同概念。资源传输尖峰可以影响排队、并发和观察，但不应误触发清单位、暂停玩法、换图或永久阻塞入服；破坏性性能措施只能由与其直接相关的游戏状态证据驱动。

### 4.3 失败时可恢复，优先放行

门控、队列、监控、热加载、反射兼容、异步任务和协调器应有界、可取消、有超时，并在异常、离线、换图、重置和卸载时释放状态。无法取得可靠状态时，宁可回退原生流程或放行，也不能永久锁住新玩家、主循环或后续任务。

### 4.4 一个职责尽量只有一个权威执行者

实验入口、兼容入口和旧入口不应与正式系统并行改变同一类状态。新增机制前先确认现有执行器、缓存和回滚链路，避免两套清理、同步、暂停或权限判断互相覆盖。

### 4.5 兼容层隔离版本差异

版本专属 API 尽量集中在兼容层并安全降级；可选端、数据库驱动和脚本类加载器之间只暴露稳定的数据契约。升级游戏、MindustryX 或 ScriptAgent 时，应先确认上游实际变化，不因版本号变化就自动复制旧方案。**是否跟进新版本由用户决定：Agent 发现上游新版时不主动升级开发基线，只记录差距并提示用户决策。**

### 4.6 权限最小化与数据边界

权限按层级和真实主体在执行时复核，敏感操作尽量可审计、可撤回；运行数据、数据库、日志、缓存、外部内容和备份不默认进入源码提交，也不因清理缓存而误删业务数据或回滚材料。

### 4.7 历史方案不能自动复活

曾经存在的代码、旧端接口或聊天提案不代表当前版本仍适用。恢复历史功能前要重新确认版本兼容、生命周期、卸载行为、玩家状态和真实风险；被明确否决的方向应保留原因，防止后续 Agent 误恢复。

### 4.8 脚本开发与版本跟进原则

- 性能优化相关改动应尽量**可靠、侵入小**，减少每次跟进新 JAR 版本后需要重新修改脚本逻辑的量；
- 脚本编写注重**兼容性、安全性、可靠性**，尽量写能兼容 Mindustry 后续更新的脚本；
- **非特殊情况不要添加过多冗余兼容与回退脚本**；最多允许到用户要求的情况——同时支持官方 Mindustry 服务端与 MindustryX 服务端。

## 5. 功能领域与当前状态地图

记忆文档只保留领域级状态；实现细节、阈值、指令和文件清单以专项文档为准。

| 领域 | 当前高层状态 | 主要风险/验证边界 | 详细真源 |
|---|---|---|---|
| 版本与上游兼容 | v159.7/B480 是当前开发候选，保留兼容降级思路 | 候选文件和生产实际版本需现场核对；上游更新不能与业务迁移混为一轮 | [官方兼容层](official-v159-compat.md)、[上游复核](upstream-review-2026-07-16.md) |
| ScriptAgent 与运行时 | 脚本已迁移到 3.4.0 的模块/数据库模型；并行加载竞态已由上游维护者修复，未发行构建已本地验证 | 尚需等待正式发行版并核对对应提交/版本；未确认生产采用，类加载器与集中卸载边界仍需保守处理 | [数据库说明](database-system.md)、[竞态 Issue 与修复验证](issue-scriptagent-load-race.md) |
| 网络同步与性能保护 | 已把网络传输压力和游戏性能压力分开；首次入服插件门控已移除；保护措施以原生语义和可恢复性为优先 | 真实多人、高上行、断线、换图、重同步和长时间运行仍需生产/测试副本证据 | [网络同步](v159-network-sync.md)、[性能保护](performance-guard.md) |
| Data Assets、CP 与热加载 | 外部 CP 支持多种数据资产形态；动态 Content 重载前会清除旧DP运行态引用，并同步修复旧建筑物品/液体模块容量及原版卸载器静态物品缓存 | 大型真实包、内存、上行、回滚耗时及多人卸载仍属观察项；“附身DP单位后卸载”仍未做破坏性回归 | [Data Assets/CP](v159-data-assets-hot-reload.md) |
| 音乐与音效 | 依赖 v159 资产/完整重同步链路，已做队列和风险收敛；不应把完整世界握手当作轻量单资产操作 | 运行中新增资产可能重置玩家状态，属于实验/受限能力，生产行为需谨慎验证 | [兼容层](official-v159-compat.md)、[网络同步](v159-network-sync.md) |
| 地图与杂交玩法 | 159/B477 后主动收缩到重点地图/标签和已维护玩法，旧地图不因历史存在就自动恢复 | 重点玩法的真实客户端、卸载、换图和长时压力回归仍按需进行 | [地图清单](map-scripts.md)、[杂交设计](hybrid-system-design.md) |
| 账号、数据库、社区与经济 | 旧服务器测试模式已废弃；五项通用服务器设置之外，数据库业务又拆为总开关与九个独立开关；已绑定默认等级同时约束信任与资历，H2磁盘预热可运行中管理 | 总开关不物理断库且不影响账号/权限/封禁/风控；多人真实负载、慢 IO、生产大库和游戏内菜单交互仍需受控验证 | [账号](account-system.md)、[数据库](database-system.md)、[信任系统](trust-system.md)、[技能系统](skill-system.md) |
| 权限、安全与管理 | 风控、封禁、最小权限协管和目标边界已收口，强调执行时复核 | 真实敏感数据上的解封/破坏性操作不由 Agent 擅自点击；生产权限需用户确认 | [安全风控](security-guard.md)、[帮助菜单](help-menu.md)、[信任系统](trust-system.md) |
| 运维与自动恢复 | 进程异常退出自动重启和退避已实现；H2磁盘预热默认开启并可用 `/diskwarmup` 管理；文档/日志/监控持续维护 | 磁盘预热只能缓解休眠后的首次数据库IO，不是TPS优化器；监督器不能替代活进程卡死监控；真实生产启停和回滚由用户/运维执行 | [脚本维护总览](scripts-maintenance.md)、[数据库说明](database-system.md) |

## 6. 重大变化时间线（高层）

详细逐次改动保留在 scripts-maintenance.md 和 Git 历史；这里记录会改变后续判断的阶段性变化。

- **2026-06**：技能、杂交、账号、信任、经济、社区和数据库等领域逐步整合，项目从零散脚本进一步形成完整产品，维护文档开始作为长期入口。
- **2026-07-07 前后**：迁移到 v159 Data Assets，重新处理音频、点歌、CP 和兼容桥；确认运行中资产同步与普通轻量数据发送不是同一件事。
- **2026-07-16～17**：围绕 B477/v159.6 和 ScriptAgent 3.4.0 进行上游复核、模块/数据库迁移与重点地图收缩；旧端同步方案不再直接套用。
- **2026-07-18～21**：网络压力、游戏同步和性能措施重新分层，MindustryX B480/v159.7 候选形成批量统计钩子、原生快照保护和统一重同步协调的当前架构。
- **2026-07-22**：外部 ZIP Data Assets 安全热加载/回滚、异常退出重启、统一封禁和最小权限协管等功能完成本地验证；仍未完成真实多人高上行验证。
- **2026-07-25**：移除可能永久卡住新玩家的插件入服门控，恢复压力保护的异常隔离和陈旧状态保护；同时定位 ScriptAgent 3.4.0 并行加载竞态，形成 Issue 材料和临时 workaround。
- **2026-07-26**：建立本长期记忆文档；后续重大上游/游戏版本变化、用户偏好变化和部署/验证状态变化应在这里留下摘要。
- **2026-07-29**：ScriptAgent 维护者已修复并行加载时过早公开 `inst`/未就绪 `classLoader` 的竞态；尚未发行的维护者构建已替换进本地项目，两轮连续启动均为 154/150/146/0，原 `ScriptClassLoader.kt:24` NPE 未复现。
- **2026-07-29**：`/cp` 扩展为v159完整DP查看与服务器CP快速加载；纯净模式改为当前局即时开关并增加管理员技能；旧服务器测试模式被彻底删除，拆分为五项持久化服务器功能设置。隔离副本完成160账号只升不降迁移验证，当前代码再完成两轮154/150/146/0启动。
- **2026-07-30**：直接运行 `mdtserver/start-server.ps1` 复核正式日志链路；`config/logs/log-0.txt` 与 `config/logs/script-load/current.log` 均随启动更新并完整记录 154/150/146/0、`Server loaded` 和逐脚本加载信息。后续诊断优先使用该链路，根目录一次性 `.tmp*` 测试日志与辅助脚本已清理。
- **2026-07-30**：`/vote` 列表改为彩色短说明，`/help` 增加按玩家实际权限过滤的指令搜索；3级“导弹齐射”改为一局一次并禁止 E 星三种核心机；新增地图 `[@pure]` 纯净模式标签。最终冷启动 155/151/146/0，标签脚本启用/停用实测通过，并修复 ScriptAgent 3.4 下 `/unloadmapscript` 缺少事务上下文的兼容问题。
- **2026-07-30**：默认绑定等级扩展为信任/资历共同下限并加入旧库一次性资历补齐；`/serverfeatures` 拆为五个可搜索管理根指令；原H2预热逻辑增加 `/diskwarmup` 运行时管理；三级技能费用按新规则调整；洪水/Lord停用链路增加显式事务、最终状态检查和标签回滚。仍以155/151/146/0冷启动通过，并完成相关控制台/命令 Socket 实测。
- **2026-07-30**：针对“玩家附身DP新增单位后卸载DP会导致全员掉线”，在 v159 Content 注销前增加中央运行态清理：先解除附身，再清理旧DP单位/建筑/地形/物品/液体/状态/载荷/规则等引用，失败则阻止注销。真实包过程暴露并修复 `Building.getPayloads()` 可空差异；最终冷启动 155/151/146/0，按用户要求未再继续破坏性附身卸载回归。
- **2026-08-01**：生产日志定位到 DP 新增物品后旧 `ItemModule` 仍保持原版长度，核心邻接更新按新物品 ID 读取时越界崩服。热重载链路现会在注销前、加载后及短期守护期同步修复 `ItemModule.empty`、活跃建筑和 `tile.build` 中暂时离组建筑的物品/液体容量；地图24662加载真实 `仙古测试2.9.1.zip` 44秒及卸载回归均为 `bad=0`，核心邻接更新未崩溃，冷启动仍为155/151/146/0。
- **2026-08-01**：新增数据库业务总开关及在线时长、成就、Wiki、MDC转账/红包、最近玩家、信任自动调整、资历自动调整、排行榜、玩家资料显示九个子开关。总开关为覆盖层，不关闭数据库连接，不影响账号/权限/封禁/禁言/IP风控/性能保护；子开关复用 `MdtSettings`，无DDL。命令 Socket 已逐项验证关闭/开启及跨重启落盘，最终正常冷启动为156/152/147/0且十项全部开启。
- **2026-08-02**：生产 DP 卸载堆栈进一步定位为原版 `Unloader.allItems` 静态快照残留，而非 `ItemModule` 容量修复遗漏。运行态缓存刷新现覆盖属性 Patch、世界加载、外部 DP 正常应用与失败回滚，并清除旧卸载器筛选；真实 `仙古测试2.9.1.zip` 验证 `unloaderItems=22->27->22`，卸载后20秒无越界，冷启动156/152/147/0。
- **2026-08-03**：放行以 .png 命名的 JPEG 伪装贴图。旧校验只认 PNG magic，`饱和火力4.0.4.2程序转换无平衡保证.zip` 的 `sprites/gked.png`（实为 JPEG）曾导致整包被拒；`externalCpHotReload.kts` 现按真实图片头校验（PNG 分支不变，非 PNG 走 JPEG 头/尾/SOF 尺寸解析），已提交 `ed576b0`。命令 Socket 实测该真实包加载成功（2141 张贴图全进资产表，`items=37`，无回滚）。
- **2026-08-04**：性能优化系统广播自然化并按局去重（提交 `9c4b63e`）。`serverPressure.kts` 删除自行播报链路，只在等级变化时记日志；玩家提示统一由 `serverPressureActions` 的进入等级/恢复广播负责，持续清理只按 30 秒聚合写日志不再刷屏，PPS/严重上行/L4 前三清理提示按局一次。
- **2026-08-06**：压力等级改 L1-L4 命名并统一去 `[服务器提示]` 文案；移除 L4 性能自动暂停（极端低 TPS 仍走兜底换图）；新增出波暂停管理指令与 50% 投票（`/wavepause`、`/vote wavepauseon|off`，内存态、默认开启、不落盘）。冷启动 156/152/147/0 并完成控制台阈值/开关实测。
- **2026-08-13**：工作区重组：mdtdo 服务器项目独立成目录（`mdt保留\mdtdo`），与 mod-dp-bridge、参考项目等并列；仓库根由 `mdt保留` 迁移至 `mdtdo`（历史完整保留），README 改为容器/项目双入口口径；`ScriptAgent4MindustryExt-mdtdo` 作为插件脚本开源仓库（独立 Git，origin/upstream）纳入项目目录并被服务器仓库 gitignore。
- **2026-08-13**：按用户要求移除上行压力检测（`trafficMonitor`）的官方端回退：改为直接 `import mindustryX.events.SendPacketEvent` 强类型监听，删除 `Class.forName` 反射检测、字段探测、旧 MDX 双事件去重与无调用方兼容函数；官方端不再受支持（单端 MindustryX 口径）。冷启动 156/152/147/0 通过。

## 7. 已替代或明确不应恢复的方向

以下不是实现清单，而是给续接 Agent 的防误用提示：

- 不恢复旧版本逐玩家手工实体序列化、取消原生包后统一重发或隐藏实体接管；
- 不把关键可靠状态降级为不可靠传输，也不以跨 TCP/UDP 的旧实体状态重放来“修复”同步；
- 不把世界、资产、音乐或 CP 传输当作游戏实体压力，不让它们直接触发清单位、暂停玩法、换图或永久入服等待；
- 不重新引入会因确认丢失而锁住全服新连接的插件入服预留/门控；
- 不维护第二套并行的性能清理/暂停/换图执行器；
- 不恢复上行压力检测对 `SendPacketEvent` 或官方端的依赖：`trafficMonitor` 已改为读取 Windows 网卡累计字节（仅 Windows、不支持 Linux），性能优化系统按网卡总上行驱动；重新引入包级分类/反射检测只会重新背上“每版本都要兼容”的负担。
- 不把运行中的完整世界/资产握手宣传为无副作用的单资产热同步；
- 不恢复旧的 ScriptAgent 本地字节码 workaround；当前上游维护构建仍是未发行候选，不能自动视为正式版本、稳定生产基线或已部署状态。
- 不恢复旧 `[危险]服务器测试模式`、`test:<account:id>` 临时主体、临时MDC/资历覆盖或固定结算×10；测试需求应通过已拆分的最小服务器功能开关处理。

如果未来上游提供了新的可靠能力，应重新阅读源码、验证时序和回滚行为后再决定，不因“过去曾经这样做过”直接恢复。

## 8. 当前优先关注的未决事项

这些事项的优先级会随用户目标和生产窗口变化，以下是最近核对后仍需保持可见的风险：

1. **生产状态未由 Agent 确认**：当前 HEAD、B480 候选 JAR、ScriptAgent 未发行修复候选以及 7 月 25 日后的脚本变化是否进入生产，需要用户/运维提供证据。
2. **真实运行期验证不足**：多人高上行、高单位、首次进服、死亡/附身、换队/观战、换图、断线、CP/音乐/杂交并发和长时间运行尚未由冷启动证明。
3. **ScriptAgent 修复发行与生产状态**：维护者提供的未发行修复构建已在本地两轮启动通过，但正式发行版尚未提供，生产是否采用也未确认。正式发布后仍需核对修复提交、构建版本与本地候选的差异。
4. **大型资产包和运行中重同步**：真实音乐、音效、贴图/CP 包的内存、带宽、回滚时延和玩家状态影响仍需受控观察。
   动态 Content 卸载前清理已覆盖玩家附身旧DP单位的失效引用，但“真实多人+附身DP单位+运行中卸载”仍需首次生产/受控窗口观察 `failures=0` 与客户端状态。
5. **游戏内交互仍待验证**：帖子关闭提示、社交按钮隐藏、注册验证码流程、服务器功能/数据库业务菜单、数据库业务关闭提示与资料隐藏、`/cp` 新DP菜单排版、纯净模式管理员技能、`/vote` 新排版、`/help` 搜索和导弹齐射的客户端提示需要真实客户端受控验证；控制台、数据库副本和冷启动不能代替该证据。
6. **生产大库迁移边界**：本地160账号的一次性信任/资历补齐已通过，但生产数据量更大；首次部署前仍应备份数据库，并观察单事务扫描耗时与磁盘IO。
7. **文档状态可能漂移**：临时任务、current-issues.md、专项文档和工作区运行产物的更新时间不同；接手时应以最新证据重新核对，而不是把“暂无待办”理解成“没有风险”。

## 9. 给新会话 Agent 的柔性接手提示

这部分是导航建议，不是不可变的审批清单：

- 先用本文档和 README 建立整体认识，再阅读与当前任务直接相关的专项文档和最新会话摘要；
- 先核对工作区、版本、JAR、加载日志、未提交改动和生产状态，确认是否存在前一轮遗留；
- 对大范围或高风险改动，优先说明影响、取舍、验证范围和回滚思路；已明确授权的小改动可直接推进；
- 测试深度按风险选择：至少让报告能说明改了什么、在哪个环境验证、覆盖了什么、还缺什么；冷启动通过不自动代表生产通过；
- 行为、逻辑、版本基线或长期决策变化时，通常在对应专项文档或本文档留下简短记录；README 主要保持入口和当前基线，不复制完整历史；
- Git 提交、远程推送和生产操作分别记录，按用户当前授权处理；运行数据、缓存、备份和真实内容不要夹带进源码提交；
- 会话结束时留下可追溯的完成项、证据、未决风险、文档状态、Git 状态和需要用户/运维执行的事项。

## 10. 何时更新这份记忆

不需要把每个小方法或每次聊天都写进来。下列变化通常值得更新“当前快照”、时间线或状态矩阵：

- Mindustry、MindustryX、ScriptAgent 或其他上游/运行时发生较大升级；
- 网络、数据库、权限、资产、性能或其他跨模块架构发生变化；
- 用户新增或修改长期偏好、授权边界、风险容忍度；
- 生产部署、回滚、测试环境或验证证据发生变化；
- 某个旧方向被否决、替代、恢复或重新评估；
- 新会话发现当前文档与代码/日志不一致。

更新时保留日期、证据来源和状态标签；旧结论如果仍有解释价值就标记为“历史/已替代”，不要悄悄改写成从未发生过。

## 11. 主要文档索引

- [README 入口](../README.md)：项目简介、目录入口和候选基线。
- [脚本维护总览](scripts-maintenance.md)：逐次脚本/模块变更、验证和维护记录。
- [官方 v159 / MindustryX 兼容层](official-v159-compat.md)：版本差异、兼容原则和历史迁移。
- [网络同步与完整重同步](v159-network-sync.md)：同步故障模型和不可违反的不变量。
- [统一性能与网络保护](performance-guard.md)：压力分层、可逆措施和诊断。
- [Data Assets / 外部 CP](v159-data-assets-hot-reload.md)：资产格式、校验、热加载和回滚边界。
- [数据库说明](database-system.md)：持久化、服务注册和类加载器隔离。
- [服务器状态统计与 Web](server-status-stats.md)：统计口径、存储、线程/缓存设计、开关与独立 Web。
- [Agent 调试经验与常见坑](agent-debug-experience.md)：服务器冷启动/启动器、编码、ScriptAgent 编译、H2/Exposed、验证与提交惯例——**新会话先读**。
- [地图脚本清单](map-scripts.md)：当前重点地图、兼容范围和恢复门槛。
- [上游复核](upstream-review-2026-07-16.md)：历史版本比较；当前结论以更新的 B480/v159.7 文档为准。
- [ScriptAgent 竞态 Issue 与修复验证](issue-scriptagent-load-race.md)：记录原始问题、本地 workaround、维护者修复与未发行构建的验证证据。

各领域的账号、信任、技能、商店、成就、帖子、Wiki、私聊、排行榜、安全和管理细节，继续以 docs 下的专项文档为准。

## 12. 本文档更新记录

- **2026-09-12（第四批，实验功能）**：尝试基于 **Mindustry 160.1 的服务端下发自定义菜单**（`Call.menuBuilder` +
  `mindustry.ui.builder`）把 wiki、帖子列表、商店、成就改成"论坛式"界面（新增两个 `coreMindustry/lib/*.kt` 库文件并接入四个入口）。
  **当日按用户实测反馈全部回退**：按钮缩放异常、界面无法适配、缺失贴图——"实际几乎全是问题"。
  四个脚本已还原到接入前版本，两个库文件已删除；复测冷启动 `158/154/149/0` 与接入前一致。
  结论与踩点保留在 [自定义菜单（尝试与回退记录）](custom-menu.md)（含一条重要环境事实：**脚本(.kts)之间不能互相 import 顶层函数**，
  跨脚本可见的只有 `lib/*.kt`）。**该方向在解决"贴图存在性 + 尺寸适配"之前不再重启。**
- **2026-09-12（第三批）**：suffixmark 自定义后缀标记从 `@Savable`（仅会话内热重载、重启即丢）改为
  **落库持久化**（`MdtSettings` 键 `suffix.customMarks`）；新增出生点保护 `spawnOverlapError` 并接入
  3x3/4x4 核心区、初级/标准预制防线、物品源、E星核心共 6 个技能（用户反馈这些技能会把敌人出生点卡掉）；
  修复雷达技能"地图没开雾就直接拒绝"导致实际不可用的问题。详见 `scripts-maintenance.md` 同批条目。
- **2026-09-12（第二批）**：功能新增与缺陷修复五项。**①禁投票**：`/votestartban`/`/voteunban`/`/votelist`（3++/4级；状态经脚本 config 持久化），玩家信息菜单（双击玩家）新增"禁止/解除ta发起投票"按钮；只拦截发起、不影响参与他人投票。**②`/vote sc`** 改为 `/vote sc [文字] [秒数]`，中屏最多 5 秒、缺省 3 秒；因原版 160 的 `Call` 无带时长 announce，改用 `setHudText` 每 250ms 刷新 + 到时 `hideHudText`。**③协管快捷菜单**（新增 `wayzer/cmds/quickOps.kts`，`/quickops`）：3++/4级列出**全部投票项**并**一键直接执行**（等价免投票通过），执行走 `RootCommands.handleInput` 与原路径同权限校验；需参数的项提示改用指令。**④性能优化系统**：核实 160 把实体纳入世界流（`NetworkIO` 新增 `writeEntityMapping`/`writeWorldEntities` + 读取侧 `SaveReadState`）对本项目**无需改代码**（流量三口径同源网卡、世界流不得触发清单位，已写入 `v159-network-sync.md`）；**修复波次卡死**——`state.wavetime` 单位是游戏刻，原值 `60f*60f*10f`=10 分钟且恢复守卫在快照为 0 时永不成立，现收敛为 30 秒并改为哨兵式恢复（三处旧守卫全改）。**⑤技能**：二级"随机永久buff"→"随机永久效果"，70% 增益/30% 负面（排除缴械/定身/无敌/动态）。新增指令已按用户要求全部进 `/help` 对应分区并分类。
- **2026-09-13**：**跟进 MindustryX X37 + 移除"遍历全部实体找火焰"的全部代码 + 热循环全量扫描审查**。**①X37**：`v2026.09.X37` 与 prerelease `B493` 是同一提交 `dc388e9`，游戏版本仍是 **v160.1**（无需破坏性适配）；`server-2026.09.X37.jar`（SHA-256 `F1CD2B5A…B575`）已放入 `mdtserver/` 并把 `server.properties` 的 `jar=` 切到它，B491/B485 保留为回滚线。**②撤回 09-12 的 `Groups.all` 火焰口径**：`limitFire.kts` 整脚本删除（它每 tick 全量扫实体数火焰；也是 09-12 每 tick 抛 `NoSuchFieldError` 的现场），`performanceGuard.kts`/`serverPressureActions.kts` 的 `clearFires()` 与相关计数/文案一并删除（**保留 O(1) 的 `state.rules.fire=false`** 与恢复项），`skills.kts` 灭火技能改为纯 tile 实现（`Fires.has/extinguish/remove`，删掉 `removedTiles` 去重集），技能本体（海啸水弹 + 10 格兜底）保留。脚本总数 158 → 157。**③审查**发现 `externalCpHotReload.kts` 的 DP 模块容量守卫在守护期内**每游戏帧**全量扫描建筑（30 秒 ≈ 1800 次 `Groups.build.toList()`），已降频到 250ms（≈4 次/秒，扫描量降约 15 倍，语义不变）；其余每帧监听器与清理轮均无 `Groups.all`，唯一保留处在 `contentsTweaker.kts` 的 DP 卸载冷路径（必须按类型筛实体）。**④验证**：B491 与 X37 冷启动均为 `157/153/148/出错0`、无异常、Socket 正常、端口全释放。详见 `scripts-maintenance.md` 同日条目、`official-v159-compat.md`、`performance-guard.md`。
- **2026-09-12**：**跟进 Mindustry 160 / MindustryX B491 并完成插件适配**。上游发布 v160.1，MindustryX 提供 `prerelease-2026.09.11.B491`（`build=160.1`；当时正式版 X36 仍是 v159.7，**160.1 的正式发行版直到 09-13 的 X37 才出现**）；参考项目已重新 clone 覆盖（`Mindustry-master` v160.1、`MindustryX-main` main + 子模块 Arc v160.1 / work 84cdf2b，另新增 `ScriptAgent-latest` 用于跟进上游适配进度）。**160 移除了生成的 `Groups.fire` 与 `Groups.puddle` 实体分组**（火焰/洼地改为按 tile 存储），插件 5 处引用随之编译失败并级联 19 个脚本；当时统一改为遍历 `Groups.all` 按实体类型筛选（`limitFire` 即时修复用户提出的"灭火/关闭火焰"失效问题——生产侧表现为每 tick `NoSuchFieldError` 打崩主循环，对应上游 issue [#49](https://github.com/way-zer/ScriptAgent4MindustryExt/issues/49)）。冷启动 `157/153/148/0` 与 159 基线一致，火焰链路运行期实测当时通过。**⚠️ 其中的 `Groups.all` 口径与 `limitFire.kts` 已于 09-13 按用户要求整体撤回（见上一条）。** 详见 `scripts-maintenance.md` 同日条目。
- **2026-09-11**：服务器状态统计改版（schema 2，Web 重做）。新增 `MdtStatsDaily`/`MdtStatsHourly`/`MdtStatsActivePlayers` 三张行数有界的明细表（每日 1 行 + 每天最多 24 行 + 每主体每天 1 行），据此产出**近 14 天人数折线图**与**今天 24 小时折线图**；新增聊天条数、游玩时长（会话口径）、峰值在线（今日/历史）、社区互动（帖子/评论/点赞/认可，累计值一次性回填历史、近 14 天值按日期区间聚合现有业务表）；**彻底移除 MDC 发放统计**（脚本不再监听 `MdcGrantedEvent`，JSON 不再输出 `mdcGranted`，历史遗留设置键保留原值不再更新）。写入压力进一步降低：由"每 60 秒无条件写 12 个设置键"改为"只有计数变化才批量落盘"，社区聚合每 5 分钟一次。`stats-web/` 重做为「履历视图 + 运维视图」双视图，页脚与引用块声明"统计信息不代表全部准确数据"。详见 `server-status-stats.md` 与 `scripts-maintenance.md` 同日条目。
- **2026-09-01**：投票比例调整（`/vote infinitefire` 与 `/vote infinitefirepromax` → 80%，`/vote pauseWave` → 70%）；3++ 开放风控菜单（用户明确**完全开放、不做隐藏/分层**：与4级相同，含增强风控/重置/IP封禁解封/今日游客观战开关）与**全部解/ban权限**（移除“只能解除自己施加的封禁”与3++单次7天上限，保留“只能处理低于3++目标”的分层边界）。详见 `scripts-maintenance.md` 同轮条目、`trust-system.md`、`security-guard.md`、`help-menu.md`。
- **2026-08-22**：按用户要求新增 [Agent 调试经验与常见坑](agent-debug-experience.md)（服务器冷启动启动器陷阱：.NET Task 读 stdout 管道会静默死锁、.cmd/.ps1 编码、jstack 定位卡死、H2/Exposed 查询、命令 Socket 协议、提交推送惯例等），并在 §11 索引。
- **2026-08-22**：移除普通风控模式的聊天限制（增强风控与强制游客观战的风控保留输入限制）；一票否决 `/veto` 提升到 3++；`/team` 自换队提升到 3++，新增 3+ 仅PVP换队指令 `/pvpteam`（参考原版 SA betterTeam）；世界处理器编辑权限防护：换图自动锁定 `allowEditWorldProcessors`（可配置）+ 权限链路复查结论（指令权限与菜单快速跳转均无越权；上游 LogicBlock 编辑只按“队伍 + privileged/allowEditWorldProcessors 全局规则”，无按玩家等级的管理员校验）。详见 `scripts-maintenance.md` 同轮条目与 `trust-system.md`。
- **2026-08-22**：新增“服务器状态统计与独立 Web”：服务器侧统计脚本 `wayzer/ext/serverStats.kts`（事件投递 + IO 协程单写者内存计数 + `MdtSettings`/`MdtStatsPlayers` 增量存储，不做全库 COUNT，默认开启，`/serverstats status|on|off`），新事件 `MdcGrantedEvent`（只统计向已登录账号的新发放MDC，排除游客与转账/红包/读博等存量流转），独立 Web `stats-web/`（`index.html` + `start-web.ps1`，默认 127.0.0.1:8081，与服务器统计开关解耦）。冷启动 157/153/148/0，JSON 生成与开关、Web 访问实测通过。详见 `docs/server-status-stats.md`、`database-system.md` 与 `scripts-maintenance.md`。
- **2026-08-22**：技能/纯净模式/玩家指令更新：鱼鱼技能改为消耗2 MDC且被纯净模式禁用；新增二级技能“随机永久buff”（可识别Buff正向池不含无敌、无限时间、10 MDC、120秒冷却、PVP与noskill禁用）；`/logout` 纳入玩家指令列表。帖子系统“关注+通知”按用户要求**放弃**。用户明确“每次更新内容都要跟进文档”，已作为长期规则写入 §3。
- **2026-08-14**：新增账号退出登录指令 `/logout`（结束会话+解除本机自动登录，不影响账号数据）并纳入账号菜单；三位ID→UUID 缓存延长至 1 天，`/pay`、`/accountqq` 等指向玩家指令可经三位ID打到 1 天内离线玩家。
- **2026-08-14**：跟进 B485 官方发行版 JAR（`server-2026.08.12.B485.jar`，SHA-256 `4C9C...09A5`，不再打 MDT 自定义补丁）；修复 `trafficMonitor` `const val` 编译错误并冷启动验证 156/152/147/0，上行网卡统计实测生效。同步 README、official-v159-compat、v159-network-sync、scripts-maintenance（B480 补丁记录转为历史）。
- **2026-08-13**：按用户要求将上行流量统计改为 **Windows 网卡计数器**（`netstat -e`，仅支持 Windows、不支持 Linux）：`trafficMonitor` 彻底移除 `SendPacketEvent` 依赖与包级分类，总上行/同步上行/世界流三口径同源，性能优化系统按网卡总上行驱动网络保护与清理。同步 README（声明不支持 Linux）、`performance-guard.md` 流量口径、`official-v159-compat.md` §3 与 `scripts-maintenance.md`；未验证，待跟进 B485 时统一验证。
- **2026-08-13**：按用户要求移除上行压力检测（`trafficMonitor`）官方端回退——直接强类型依赖 MindustryX `SendPacketEvent`，删除反射检测/字段探测/旧 MDX 去重与无调用方兼容函数；官方端不再受支持。同步 `official-v159-compat.md` §3 与 `performance-guard.md`，冷启动 156/152/147/0 通过。
- **2026-08-13**：按用户修正口径：A) 版本跟进总体原则——跟进新的稳定版本，JAR/SA 插件均由用户决定跟进哪个版本；B) 推送前先同步 docs 与 scripts 改动，推送默认包含 scripts 与 Agent 开发文档；D) 新增脚本开发原则（§4.8）：性能优化可靠低侵入、注重兼容性/安全性/可靠性、尽量兼容后续版本，非特殊情况不加过多冗余兼容与回退脚本，最多允许用户要求的官方端与 MindustryX 服务端双支持。
- **2026-08-13**：参考项目跟进上游（服务器候选基线不变）：Mindustry→origin/master（`275d357`，比 v159.7 多 172 提交）、MindustryX→`prerelease-2026.08.12.B485`（`a885601`，work/Arc 子模块已同步）、ScriptAgent→v3.4.0 之后的 origin/master（`c5555da`，+9 提交）；新增 `参考项目/README.md`（仓库地址、跟进步骤、代理回退 10808→7897）；移除 `mlogjs-main`。
- **2026-08-13**：明确远程推送流程（服务器仓库无远程、不直接推送；改动先同步到 `ScriptAgent4MindustryExt-mdtdo` 再推送）；移除不再需要的 `柠檬的开源插件` 与 `session浓缩` 相关引用；核对插件仓库与服务器脚本树内容一致（35 个文件仅 CRLF/LF 换行差异，21 个仅服务器侧存在的为运行数据）。

- **2026-08-13**：记录工作区重组：仓库根迁移至 `mdtdo/`、`mdt保留` 改为多项目容器（README 双入口口径）、`ScriptAgent4MindustryExt-mdtdo` 插件开源仓库纳入项目目录；同步维护对象、工作区、主要区域与文档索引路径。
- **2026-08-13**：补记 08-04/08-06 三个 `serverPressure` 提交（广播自然化与按局去重、移除性能自动暂停、L1-L4 命名、出波暂停投票与管理开关）并同步 `performance-guard.md`；现场核对基线（HEAD `2688554`、候选 JAR SHA-256 与文档一致、ScriptAgent 修复构建在位、无残留进程/端口占用、最近一次冷启动 156/152/147/0）并在快照登记 `mod-dp-bridge` 独立转换工具仓库。同步用户明确的测试边界：默认只做冷启动脚本加载验证与启动后控制台/Socket 可测项，不碰真实客户端、不手动加压，无法经控制台测试的改动按未覆盖边界记录。
- **2026-08-02**：同步 DP 卸载后原版卸载器静态物品缓存残留的根因、刷新/校验策略和真实包加载卸载验证。
- **2026-08-03**：同步“以 .png 命名的 JPEG 伪装贴图”修复及其实测证据；在“启动与脚本加载日志优先级”补充本地服务器启动、命令 Socket 竞态规避、自动化测试与退出清理要点。
- **2026-08-01**：同步 DP 动态物品导致旧 `ItemModule` 越界崩服的根因、容量修复/短期守护方案和地图24662长时加载卸载验证。
- **2026-07-30**：同步 v159 动态 Content 卸载前运行态清理、DP附身全员掉线风险、可空载荷兼容修复与最终验证边界。
- **2026-07-30**：同步默认信任/资历共同下限、五个拆分管理指令、H2磁盘预热管理、三级技能费用、洪水/Lord停用修复和最终验证证据。
- **2026-07-30**：同步投票列表可读性、帮助搜索、导弹齐射限制、`[@pure]` 地图标签、155/151/146/0 冷启动与标签启停验证证据。
- **2026-07-30**：记录正式启动/脚本加载日志的优先使用规则、直接启动复核证据和根目录临时产物清理结果。
- **2026-07-29**：同步 `/cp` v159 DP/快速加载、当前局纯净模式、服务器测试模式废弃、五项服务器功能设置、数据库副本迁移与最终两轮启动验证。
- **2026-07-29**：同步 ScriptAgent 并行加载竞态的上游修复状态、未发行构建替换、两轮本地启动证据和待正式发行/生产确认边界。
- **2026-07-26**：根据四个会话浓缩、README、维护总览、兼容/网络/性能/数据库/资产文档和当前 Git 状态建立；加入用户协作偏好、生产边界、重大历史阶段、已替代方向和当前未验证项。
